# Eventiya - Event Management & Registration System

This is a university project developed by GP28.

## Project Structure
- `backend/`: Java Spring Boot application (Maven).
- `frontend/`: React application (Vite).

## Getting Started

### Prerequisites
- Java 17 or higher
- Node.js 18 or higher
- MySQL Server

### Running the Backend
1. Navigate to the `backend/` directory.
2. Configure your database in `src/main/resources/application.properties`.
3. Run `mvn spring-boot:run`.

### Running the Frontend
1. Navigate to the `frontend/` directory.
2. Run `npm install`.
3. Run `npm run dev`.

## Team Distribution (Sprint 1)
- **Ashen**: Project Lead & RBAC (Story 3).
- **Member 2**: User Registration (Story 1).
- **Member 3**: User Login (Story 2). Profile Management (Story 4)
- **Member 4**: Design & QA/Testing & DevOps

## Commit Guidelines
- Use descriptive commit messages.
- Always push to your feature branch and create a PR.
